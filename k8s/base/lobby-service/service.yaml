apiVersion: v1
kind: Service
metadata:
  name: lobby-service
  labels:
    app: belote
    component: lobby-service
spec:
  type: ClusterIP
  selector:
    app: belote
    component: lobby-service
  ports:
    - name: http
      port: 3000
      targetPort: 3000
