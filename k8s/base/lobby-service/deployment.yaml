apiVersion: apps/v1
kind: Deployment
metadata:
  name: lobby-service
  labels:
    app: belote
    component: lobby-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: belote
      component: lobby-service
  template:
    metadata:
      labels:
        app: belote
        component: lobby-service
    spec:
      containers:
        - name: lobby-service
          image: ghcr.io/jimmmmothy/lobby-service:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: belote-config
            - secretRef:
                name: belote-secrets
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: belote-secrets
                  key: LOBBY_DATABASE_URL
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            exec:
              command: ["/bin/sh", "-c", "pgrep node"]
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            exec:
              command: ["/bin/sh", "-c", "pgrep node"]
            initialDelaySeconds: 20
            periodSeconds: 20
