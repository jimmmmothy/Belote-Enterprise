apiVersion: v1
kind: Service
metadata:
  name: game-service
  labels:
    app: belote
    component: game-service
spec:
  type: ClusterIP
  selector:
    app: belote
    component: game-service
  ports:
    - name: http
      port: 3000
      targetPort: 3000
