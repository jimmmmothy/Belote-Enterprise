apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: lobbies-postgres
  labels:
    app: belote
    component: lobbies-postgres
spec:
  serviceName: lobbies-postgres
  replicas: 1
  selector:
    matchLabels:
      app: belote
      component: lobbies-postgres
  template:
    metadata:
      labels:
        app: belote
        component: lobbies-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:15
          ports:
            - containerPort: 5432
              name: postgres
          env:
            - name: POSTGRES_USER
              valueFrom:
                configMapKeyRef:
                  name: belote-config
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: belote-secrets
                  key: POSTGRES_PASSWORD
            - name: POSTGRES_DB
              valueFrom:
                configMapKeyRef:
                  name: belote-config
                  key: LOBBIES_POSTGRES_DB
          volumeMounts:
            - name: lobbies-postgres-data
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command: ["/bin/sh", "-c", "pg_isready -U $POSTGRES_USER"]
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            exec:
              command: ["/bin/sh", "-c", "pg_isready -U $POSTGRES_USER"]
            initialDelaySeconds: 20
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
  volumeClaimTemplates:
    - metadata:
        name: lobbies-postgres-data
        labels:
          app: belote
          component: lobbies-postgres
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 1Gi
