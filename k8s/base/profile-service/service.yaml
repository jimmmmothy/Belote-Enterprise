apiVersion: v1
kind: Service
metadata:
  name: profile-service
  labels:
    app: belote
    component: profile-service
spec:
  type: ClusterIP
  selector:
    app: belote
    component: profile-service
  ports:
    - name: http
      port: 3000
      targetPort: 3000
