name: Game Service CI/CD

on:
  push:
    branches: [main]
    paths:
      - "game-service/**"
      - "k8s/base/game-service/**"
      - "k8s/overlays/minikube-game/**"
      - ".github/workflows/game.yaml"
  pull_request:
    branches: [main]
    paths:
      - "game-service/**"
      - "k8s/base/game-service/**"
      - "k8s/overlays/minikube-game/**"
      - ".github/workflows/game.yaml"

env:
  IMAGE_NAME: ghcr.io/${{ github.repository_owner }}/game-service

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: game-service
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Test
        run: npm test

  build-and-push:
    needs: build-and-test
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        run: |
          docker buildx build --push \
            -f game-service/Dockerfile \
            -t $IMAGE_NAME:latest \
            -t $IMAGE_NAME:${{ github.sha }} \
            game-service

  deploy-minikube:
    needs: build-and-push
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: [self-hosted, minikube]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Apply game overlay
        run: minikube kubectl apply -k k8s/overlays/minikube-game

      - name: Update image
        run: minikube kubectl set image deployment/game-service game-service=$IMAGE_NAME:${{ github.sha }} -n default

      - name: Verify rollout
        run: minikube kubectl rollout status deployment/game-service --timeout=120s