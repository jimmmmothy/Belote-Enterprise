{
  "scripts": {
    "dev": "docker compose up -d nats nats-exporter prometheus grafana lobbies-postgres users-postgres && concurrently -n SERVER,LOBBY,GAME,CLIENT,AUTH,PROFILE -c cyan,green,magenta,blue,yellow,red \"cd server && npm run dev\" \"cd lobby-service && npm run dev\" \"cd game-service && npm run dev\" \"cd client && npm run dev\" \"cd auth-service && npm run dev\" \"cd profile-service && npm run dev\"",
    "dev-no-server": "docker compose up -d nats nats-exporter lobbies-postgres users-postgres && concurrently -n LOBBY,GAME,CLIENT -c green,magenta,blue \"cd lobby-service && npm run dev\" \"cd game-service && npm run dev\" \"cd client && npm run dev\"",
    "test": "vitest"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "vitest": "^4.0.10"
  }
}
